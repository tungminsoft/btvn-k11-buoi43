const config = {
  routes: {
    home: "/",
    products: "/products",
    productDetail: "/products/:id",
    register: "/register",
    login: "/login",
    users: "/users",
    notFound: "*",
  },
};

export default config;
