const config = {
  routes: {
    home: "/",
    products: "/products",
    productDetail: "/products/:id",
    register: "/register",
    login: "/login",
    notFound: "*",
  },
};

export default config;
